#!/bin/bash

if [[ -z "$1" ]]; then
  echo "USAGE: $0 PORT [SUBDOMAIN]"
  exit 1
fi

if [[ -n "$2" ]]; then
  SUBDOMAIN="$2."
fi

set +v
ssh -R "${SUBDOMAIN}arko.me:80:localhost:$1" plan@localhost.run
